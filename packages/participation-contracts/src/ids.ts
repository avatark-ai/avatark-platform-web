// Sprint 19: identifiers for the visitor-participation domain. Plain
// string aliases -- matching the unbranded convention every existing
// runtime package already uses (see @avatark/encounter-realization-contracts'
// own EncounterRecordId, @avatark/canonical-event-contracts' own identity
// fields).
export type ParticipationRecordId = string
