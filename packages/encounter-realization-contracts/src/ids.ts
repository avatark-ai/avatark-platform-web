// Sprint 14: identifiers for the encounter-realization domain. Plain
// string aliases -- matching the unbranded convention every existing
// runtime package already uses.
export type EncounterRecordId = string
