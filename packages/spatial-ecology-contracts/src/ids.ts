// Sprint 16: identifiers for the spatial-ecology domain. Plain string
// aliases -- matching the unbranded convention every existing runtime
// package already uses (see @avatark/runtime-contracts's own ids.ts,
// which explicitly rejected branding to avoid touching every existing
// signature).
export type DomainId = string
export type SectorId = string
export type QuadrantId = string
export type PatchId = string
export type LocalPlaceId = string
export type SpatialEdgeId = string
export type RouteId = string
export type TerritoryClaimId = string
