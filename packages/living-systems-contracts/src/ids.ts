// WorldId/LocationId/UserId already exist in @avatark/runtime-contracts --
// deliberately not re-declared here. Only the id kinds genuinely new to
// this package live in this file.
export type SeasonId = string
export type EntityArchetypeId = string
export type EntityId = string
export type EncounterRuleId = string
