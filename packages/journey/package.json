{
  "name": "@avatark/journey",
  "version": "0.1.0",
  "private": true,
  "description": "Shared, framework-agnostic journey state machine, manifest, deep links, continuity, and handoff contracts -- the AvatarK-owned Entry Engine logic every product's journey steps run through.",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "types": "./src/index.ts",
  "scripts": {
    "test": "node --experimental-strip-types --test src/stateMachine.test.ts src/manifest.test.ts src/deepLinks.test.ts src/recovery.test.ts src/handoffContracts.test.ts src/invitationAcceptance.test.ts src/guestContext.test.ts"
  },
  "dependencies": {
    "@avatark/auth": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5"
  }
}
