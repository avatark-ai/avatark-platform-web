// Sprint 13: identifiers for the living-rhythms domain. Plain string
// aliases -- matching the unbranded convention every existing runtime
// package already uses.
export type DayPhaseScheduleId = string
export type RoutineDefinitionId = string
