# AvatarK Identity RC1 — Release Gate

This is a ground-truth checklist against the mission's Part 19 criteria, verified by re-running the actual commands, not inferred from what was written. `READY_FOR_PRODUCT_ADOPTION` is set honestly below — it is **not** `true` merely because code compiles.

## Checklist

| Criterion | Status | Evidence |
|---|---|---|
| AvatarK consumes the extracted packages | ✅ | `app/account/page.tsx` imports `@avatark/account`/`@avatark/account-ui` (workspace, not tarball); `app/auth/sign-in/page.tsx` imports `@avatark/auth-ui`. `avatark-account-0.1.1.tgz` deleted, `package.json` updated. |
| Sign-in and account visuals pass desktop/tablet/mobile review | ⚠️ **Partial** | Real headless-Chromium screenshots (Playwright) were captured at 1440×900 / 834×1112 / 390×844 for `/auth/sign-in` (idle, error, magic-link-sent — the sent state via network-level interception of Supabase's own `/auth/v1/otp` call, not a real email send), the dynamic Google-button visibility, and the public `/status` page. Dark AvatarK visual quality is preserved, no giant empty areas, no duplicated nav, mobile collapses to a single hamburger, no label collisions. `/account` and `/admin` were confirmed to correctly redirect an unauthenticated visitor to sign-in (the auth gate itself is visually verified) — but this environment has no seeded test-user credentials or service-role key, so the **authenticated** account tabs (profile/products/membership/preferences/privacy/security/data-export), avatar menu, and the admin diagnostics table could not be rendered in a real browser this session. That remaining slice is a genuine, named gap, not assumed passing. |
| Google-provider visibility is dynamically resolved | ✅ | Unchanged live-capability-probe mechanism (`lib/auth/authProviderCapabilities.ts`), now threaded through `@avatark/auth-ui`'s `shouldShowGoogleButton()`. No static `NEXT_PUBLIC_GOOGLE_OAUTH_ENABLED` dependency anywhere in the new code. |
| Magic-link flow is intact | ✅ | `MagicLinkForm`/`MagicLinkSentState` wired to the same `supabase.auth.signInWithOtp` call as before; behavior unchanged, only the UI composition changed. |
| Callback and safe return paths pass tests | ✅ | `packages/auth/src/safeReturnPath.test.ts`, `callbackError.test.ts` — pre-existing, still passing. `app/auth/callback/route.ts` untouched (already correct). |
| Product configuration exists for all nine products | ✅ | `PRODUCT_IDENTITY_CONFIGS` (`packages/product-registry/src/identityConfig.ts`) — all 9, tested (`identityConfig.test.ts`, 7 cases). |
| CinemaK and StatpointK are marked live | ⚠️ **Partial, by design** | Both are `deploymentStatus: 'live'` (DNS-reachable, correctly distinguished from "coming soon"). Neither is `accessState: 'available'` or `integrationStatus: 'live'` — that's the mission's own required distinction (deployment ≠ integration ≠ access), not an oversight. |
| Locale contract is frozen | ✅ | `@avatark/locale`: en-US/en-IN `available`, es/fr/hi/te/ta `planned`. 12 tests passing. |
| Appearance contract is frozen | ✅ | `@avatark/appearance`: system/dark `complete`, light `internal`, high-contrast `planned`. 9 products' accent tokens. 6 tests passing. |
| Membership/entitlement/role/capability contracts are frozen | ✅ | `@avatark/membership`'s `entitlement.ts` (`ProductAccess`, `resolveCapability`, default-deny). 7 tests passing. |
| Organization contract is frozen | ✅ | `@avatark/organizations`'s `context.ts` (current org, switching, org-scoped entitlement). 6 tests passing. |
| Extension-slot API is frozen | ✅ | `packages/account/src/contracts/adapters.ts`'s `ExtensionAdapter`/`ExtensionSlotContent`, rendered by `ExtensionTab`. All 9 products' extension domains registered in `identityConfig.ts`. |
| Diagnostics are protected | ✅ | The 3-tier contract (`lib/admin/diagnosticsTiers.ts`, now 11 cases incl. `buildDiagnosticsPayload`) is wired into `app/admin/page.tsx`: `getAdminContext()` resolves a tier via `resolveDiagnosticsTier`, and `buildDiagnosticsPayload` nulls the entire developer/platform-ops payload unless the tier includes it. `app/admin/layout.tsx` already gates all of `app/admin/**` on a non-null admin context before any child renders, so the tier check is additional defense-in-depth, not the only gate. New public `/status` page shows only the coarse rollup (never per-item detail). `CopyDiagnosticsButton` only ever receives the pre-redacted string from `buildSafeDiagnosticsCopy`. Independently confirmed by the dedicated security review below. |
| Secret-exposure regression tests pass | ✅ | `packages/auth-ui/src/noSecrets.test.ts`, `packages/account/src/importBoundary.test.ts`, `lib/admin/diagnosticsTiers.test.ts`'s redaction fixture — all passing. |
| Packages build and pack deterministically | ✅ | `pnpm run build:packages` (19/19) and `pnpm run pack:packages` (19/19, checksummed) both re-run clean this session. |
| Migration artifacts are generated | ✅ | `docs/migrations/IDENTITY_{GAMEK,STUDIOK,STREAMK,CINEMAK,ARENAK,ATLAS,SETPOINTK,PROMETHEUSK}.md` — all 8, plus pointers added from the 5 prior narrower guides. |
| Package versions are assigned | ✅ | `@avatark/account` 0.2.0 (ownership transfer), `@avatark/auth-ui`/`locale`/`appearance` 0.1.0 (new). See `docs/IDENTITY_PACKAGE_DISTRIBUTION.md`. |
| No open critical security defects | ✅ | Previously found and fixed: `SignInMethodsTab`'s Apple OAuth deployment-checklist leak. This session: a dedicated `/security-review` pass ran four parallel reviewers over the full RC1 diff (auth/session/callback, account/org/membership, admin/diagnostics/status, navigation/invitations/redirects) against the specific threat list — open redirects, return-path parsing, callback-origin injection, server-only data exposure, service-role/secret leakage, diagnostics authz, entitlement default-deny, org-context escalation, extension-slot bypass, unconfigured-provider exposure, unsafe HTML injection, env exposure, SetpointK's data boundary, session/sign-out behavior. **Zero high-confidence findings.** One pre-existing, already-documented, low-severity design note (the admin-only organization-member-role PATCH accepts an unvalidated role string; `packages/organizations/src/permissions.ts` already states this table isn't enforced anywhere yet) — not independently exploitable since the route is already gated to platform admins, and out of RC1's scope to fix. |
| No product mounting instructions depend on uncommitted code | ✅ | This session's changes (diagnostics wiring + `/status`) are committed and pushed to `feature/avatar-platform-rc3` — see commit hash in the push confirmation for this session. |

## Known deferred architectural risk (not a blocker, tracked honestly)

Two independent avatar-menu implementations still exist (`components/echo/shell/EchoAvatarMenu.tsx` and `@avatark/account-ui`'s `AvatarMenu`) with diverging contracts. They do not visibly duplicate on any single screen today, so this was not treated as a release blocker — but merging them is real, separate work, not yet done. See `docs/CANONICAL_ACCOUNT_SHELL.md`.

## READY_FOR_PRODUCT_ADOPTION

```
READY_FOR_PRODUCT_ADOPTION = false
```

**Why still false, precisely:** of the three gaps named in the prior pass, two are now genuinely closed — (2) diagnostics tiers are wired into `app/admin/page.tsx`'s actual rendering with `app/admin/layout.tsx` still the primary gate, confirmed by a dedicated security reviewer; (3) a four-reviewer `/security-review` pass ran against the full RC1 diff (auth, account/org/membership, admin/diagnostics, navigation/invitations) with zero high-confidence findings. Gap (1), browser-level visual verification, is only **partially** closed: the sign-in flow (idle/error/magic-link-sent states, dynamic Google-button visibility) and the new public `/status` page were verified with real headless-Chromium screenshots at desktop/tablet/mobile, and `/account`+`/admin`'s auth-redirect gating was confirmed — but the actual authenticated account tabs, avatar menu, and admin diagnostics table were never rendered in a live browser this session, because this environment has no seeded test-user credentials or service-role key to establish a real signed-in session. That is the one remaining named blocker: **obtain a way to authenticate a test session in this (or a similar) environment and visually verify the authenticated account/admin surfaces** before flipping this to `true`. Every other mission criterion is genuinely met, re-verified by re-running the actual commands this session (464/464 tests, clean lint/typecheck, clean `build`/`build:packages`/`pack:packages`) — not assumed from the prior pass.

## RC1.1 addendum (Authenticated Account Completion)

A follow-on mission ("Identity RC1.1") completed the authenticated account surface itself — Access/Organizations/Notifications sections, a truthful Products model, the product-strip "0"/"A" defect, and a real (previously undiscovered) mobile-overflow bug in the shared `EchoShell`. Full detail, its own checklist, and its own `READY_FOR_PRODUCT_ADOPTION` verdict live in `docs/IDENTITY_RC11_AUTHENTICATED_ACCOUNT.md` — read that file for anything about the account experience specifically; this file's gate above is frozen as the RC1 (auth-ui/account-shell/product-config extraction) record and is not being retroactively edited. The still-open blocker is the same one named above, narrower in scope: real authenticated-session verification of the live adapters, not just the component layer.
